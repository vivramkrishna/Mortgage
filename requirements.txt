fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
mcp==1.13.1
pyngrok==7.2.0
python-multipart==0.0.9
httpx==0.27.2
# Arrives with uvicorn[standard] too, but pinned here because the app imports
# it directly to read .env.
python-dotenv==1.2.2

# Transaction query engine (backend/transactions_data.py, transaction_query.py):
# reads the customer transaction workbook and answers natural-language queries
# without any LLM call.
pandas==3.0.5
openpyxl==3.1.5
python-dateutil==2.9.0.post0
